name: studia_payment_backend
description: Backend for Stripe payment integration.
environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  http: ^1.6.0
  dotenv: ^4.2.0
  crypto: ^3.0.7

executables:
  server:
    path: bin/server.dart
