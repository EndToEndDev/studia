name: studia
description: Local tutorlink project
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  http: ^1.4.0
  webview_flutter: ^4.13.1
  sqflite: ^2.3.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on available attributes, see the documentation.
